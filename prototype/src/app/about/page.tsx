import { AboutPageClient } from "@/components/pages";

export const metadata = {
  title: "About — SemiAnalysis",
  description:
    "Independent research and analysis on semiconductors and AI — bridging the gap between the world's most important industry and business.",
};

export default function AboutPage() {
  return <AboutPageClient />;
}
